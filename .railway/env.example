# ==========================================
# Railway 环境变量配置
# ==========================================
# 在 Railway 项目的 Variables 标签页中添加以下环境变量

# ==========================================
# GLM 智谱AI 配置 (必须)
# ==========================================
# LLM 提供商
LLM_PROVIDER=openai

# LLM 模型名称 (智谱AI)
LLM_MODEL=glm-4-flash

# 智谱API Key (在 https://open.bigmodel.cn/ 获取)
LLM_API_KEY=your-zhipu-api-key-here

# 智谱AI API 地址
LLM_API_BASE=https://open.bigmodel.cn/api/paas/v4

# ==========================================
# 服务器配置 (可选)
# ==========================================
# 后端主机地址 (默认值，通常不需要修改)
HOST=0.0.0.0

# 后端端口 (默认值，通常不需要修改)
PORT=8000

# ==========================================
# CORS 配置 (部署到 Vercel 后需要修改)
# ==========================================
# 将 your-domain.vercel.app 替换为你的 Vercel 域名
FRONTEND_BASE_URL=https://your-domain.vercel.app

# CORS 允许的源 (JSON 数组格式)
# 部署后需要将 your-domain.vercel.app 替换为实际域名
CORS_ORIGINS=["https://your-domain.vercel.app"]

# ==========================================
# 端口配置 (通常不需要修改)
# ==========================================
# 前端端口
FRONTEND_PORT=3000

# 后端端口
BACKEND_PORT=8000
